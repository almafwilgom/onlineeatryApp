// userController.js — stub (implemented in Phase 4)
const userController = {
  getProfile:    (req, res) => res.status(501).json({ success: false, message: 'Not implemented yet.' }),
  updateProfile: (req, res) => res.status(501).json({ success: false, message: 'Not implemented yet.' }),
};
module.exports = userController;
